{title}

Travis build #{buildNumber} for commit {commitID} has completed successfully and the resulting image has been deployed to the staging environment.

{commitMessage}

Diff: {commitURL}
Build log: {buildURL}
